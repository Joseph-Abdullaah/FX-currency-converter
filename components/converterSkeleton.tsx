import { Skeleton } from "@/components/ui/skeleton"

/** Placeholder for the converted amount while a conversion is loading. */
export default function ConverterSkeleton() {
  return <Skeleton className="h-10 w-32" />
}
